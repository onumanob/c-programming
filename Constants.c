#include <stdio.h>

int main()

{

    const int minutes_per_hour = 60; // variable is unchangeable and read-only and also always declare constant with value

    printf("%d", minutes_per_hour);

    return 0;

}
